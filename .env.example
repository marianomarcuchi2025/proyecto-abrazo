# Copiar a .env y ajustar antes de desplegar. Nunca commitear el .env real.

# Origen permitido para CORS. NO usar "*" en producción.
CORS_ORIGIN=http://localhost:3000

# Puerto del backend
PORT=3001
