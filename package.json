{
  "name": "proyecto-abrazo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --stream --parallel",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "lerna": "^8.1.0"
  },
  "license": "AGPL-3.0-only"
}
